import type { Metadata } from "next";
import { Inter, Outfit } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

const outfit = Outfit({
  variable: "--font-outfit",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "DMX Talent Solutions Pvt Ltd — Expert Hiring & HR Services",
  description:
    "Partner in Modern HR Solutions. We help businesses find top talent through smart sourcing, executive search, and leadership development. 300+ vacancies hiring globally.",
  keywords:
    "talent solutions, hiring, recruitment, HR services, executive search, staffing, DMX Talent Solutions",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${inter.variable} ${outfit.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
